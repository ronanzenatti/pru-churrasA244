import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pru-churrasA244',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
